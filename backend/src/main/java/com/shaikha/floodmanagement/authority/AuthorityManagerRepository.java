package com.shaikha.floodmanagement.authority;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AuthorityManagerRepository extends JpaRepository<AuthorityManager, Long> {
}
